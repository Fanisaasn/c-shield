@extends('admin.layouts.app')
@section('title', $title)
@section('content')
<div class="flex items-center justify-between"><p class="text-sm text-slate-500">Kelola data {{ strtolower($title) }} yang tampil di portal publik.</p><a href="{{ route('admin.'.$route.'.create') }}" class="rounded-md bg-teal-500 px-4 py-2 text-sm font-semibold text-navy-950">Tambah {{ rtrim($title, 's') }}</a></div>
<div class="mt-5 overflow-hidden rounded-xl border border-slate-200 bg-white"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-4 py-3">Judul</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Diperbarui</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($items as $item)<tr><td class="px-4 py-3 font-medium text-navy-900">{{ $item->title }}</td><td class="px-4 py-3">{!! $item->is_published ? '<span class="text-emerald-600">Publik</span>' : '<span class="text-slate-400">Draft</span>' !!}</td><td class="px-4 py-3 text-slate-500">{{ $item->updated_at->format('d M Y') }}</td><td class="px-4 py-3 text-right"><a class="mr-3 text-teal-700" href="{{ route('admin.'.$route.'.edit', $item) }}">Edit</a><form class="inline" method="POST" action="{{ route('admin.'.$route.'.destroy', $item) }}">@csrf @method('DELETE')<button onclick="return confirm('Hapus data ini?')" class="text-red-600">Hapus</button></form></td></tr>@empty<tr><td colspan="4" class="px-4 py-8 text-center text-slate-400">Belum ada data.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $items->links() }}</div>
@endsection
