@extends('admin.layouts.app')
@section('title', 'Monitoring & Laporan')
@section('content')
<form class="grid gap-3 rounded-xl border border-slate-200 bg-white p-4 md:grid-cols-5" method="GET"><select name="type" class="rounded-md border-slate-300"><option value="">Semua tipe</option><option value="pre" @selected(request('type')==='pre')>Pre-test</option><option value="post" @selected(request('type')==='post')>Post-test</option></select><select name="level" class="rounded-md border-slate-300"><option value="">Semua tingkat</option>@foreach($levels as $level)<option @selected(request('level')===$level)>{{ $level }}</option>@endforeach</select><input type="date" name="from" value="{{ request('from') }}" class="rounded-md border-slate-300"><input type="date" name="to" value="{{ request('to') }}" class="rounded-md border-slate-300"><button class="rounded-md bg-teal-500 px-4 py-2 text-sm font-semibold">Filter</button></form>
<div class="mt-5 overflow-hidden rounded-xl border border-slate-200 bg-white"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-4 py-3">Peserta</th><th class="px-4 py-3">Tipe</th><th class="px-4 py-3">Kategori</th><th class="px-4 py-3">Skor</th><th class="px-4 py-3">Tanggal</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($attempts as $attempt)<tr><td class="px-4 py-3"><p class="font-medium">{{ $attempt->user?->name ?? '-' }}</p><p class="text-xs text-slate-400">{{ $attempt->user?->domicile }}</p></td><td class="px-4 py-3 uppercase">{{ $attempt->type }}</td><td class="px-4 py-3">{{ $attempt->answers->first()?->question?->category?->name ?? '-' }}</td><td class="px-4 py-3">{{ $attempt->score }} <span class="text-xs text-slate-400">{{ $attempt->level }}</span></td><td class="px-4 py-3 text-slate-500">{{ $attempt->completed_at->format('d M Y H:i') }}</td></tr>@empty<tr><td colspan="5" class="px-4 py-8 text-center text-slate-400">Tidak ada hasil assessment yang sesuai.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $attempts->links() }}</div>
@endsection
